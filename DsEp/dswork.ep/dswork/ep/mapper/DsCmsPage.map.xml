<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
	"-//ibatis.apache.org//DTD Mapper 3.0//EN" 
	"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="dswork.ep.model.DsCmsPage">

<insert id="insert" parameterType="dswork.ep.model.DsCmsPage">
	<selectKey resultType="Long" order="AFTER" keyProperty="id">
		select LAST_INSERT_ID()
	</selectKey>
	insert into DS_CMS_PAGE
	(ID, SITEID, CATEGORYID, TITLE, KEYWORDS, SUMMARY, CONTENT, RELEASETIME, IMG, IMGTOP, PAGETOP)
	values
	(null, #{siteid}, #{categoryid}, #{title}, #{keywords}, #{summary}, #{content}, #{releasetime}, #{img}, #{imgtop}, #{pagetop})
</insert>

<delete id="delete">
	delete from DS_CMS_PAGE where ID=#{id}
</delete>

<update id="update" parameterType="dswork.ep.model.DsCmsPage">
	update DS_CMS_PAGE set
		TITLE=#{title},
		KEYWORDS=#{keywords},
		SUMMARY=#{summary},
		CONTENT=#{content},
		RELEASETIME=#{releasetime},
		IMG=#{img},
		IMGTOP=#{imgtop},
		PAGETOP=#{pagetop}
	where ID=#{id}
</update>

<resultMap id="result" type="dswork.ep.model.DsCmsPage">
	<id property="id" column="ID" />
	<result property="siteid" column="SITEID" />
	<result property="categoryid" column="CATEGORYID" />
	<result property="title" column="TITLE" />
	<result property="keywords" column="KEYWORDS" />
	<result property="summary" column="SUMMARY" />
	<result property="content" column="CONTENT" />
	<result property="releasetime" column="RELEASETIME" />
	<result property="img" column="IMG" />
	<result property="imgtop" column="IMGTOP" />
	<result property="pagetop" column="PAGETOP" />
</resultMap>

<sql id="columns">
	ID, SITEID, CATEGORYID, TITLE, KEYWORDS, SUMMARY, CONTENT, RELEASETIME, IMG, IMGTOP, PAGETOP
</sql>

<sql id="dynamicWhere">
	<where>
		SITEID=#{siteid}
		<if test="@Ognl@isNotEmpty(categoryid)"> and CATEGORYID=#{categoryid} </if>
		<if test="@Ognl@isNotEmpty(keyvalue)">
			 and TITLE like concat('%',#{keyvalue},'%') 
			 and KEYWORDS like concat('%',#{keyvalue},'%') 
			 and SUMMARY like concat('%',#{keyvalue},'%') 
			 and CONTENT like concat('%',#{keyvalue},'%') 
		</if>
	</where>
</sql>

<select id="select" resultMap="result">
	select <include refid="columns" /> from DS_CMS_PAGE where ID=#{id}
</select>

<select id="query" resultMap="result">
	select <include refid="columns" /> from DS_CMS_PAGE
	<include refid="dynamicWhere" />
</select>

<select id="queryCount" resultType="int">
	select count(1) from DS_CMS_PAGE
	<include refid="dynamicWhere" />
</select>

</mapper>
